# ──────────────────────────────────────────────
# SafeVoice — Dependencias base (todos los entornos)
# ──────────────────────────────────────────────

# Framework principal
# NOTA: mssql-django 1.5 requiere Django < 5.1; se usa Django 4.2 LTS.
Django==4.2.16
djangorestframework==3.15.2

# SQL Server
mssql-django==1.5

# Variables de entorno
django-environ==0.11.2

# Autenticación con JWT (preparado para siguiente etapa)
djangorestframework-simplejwt==5.3.1

# CORS (necesario para el frontend)
django-cors-headers==4.4.0

# Validación de contraseñas
password-validator==1.0

# Utilidades
pytz==2024.1
